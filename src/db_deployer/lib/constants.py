import os

# Cache filename (created inside repo root)
_CACHE_FILENAME = '.db_deployer.cache'

# Environment variable used to locate the SQL repo
_ENV_REPO = 'DB_DEPLOYER_REPO'

# Executables
_EXEC_VACUUM_FULL_PATH = 'bin/vacuum_full'
